# Комора — Прогрес розробки

> Оновлювати після кожної сесії. Відмічати `[x]` тільки після перевірки в AI Studio Preview.
> Кожен модуль має дві секції: **Реалізація** (що збудувати) і **Перевірки** (що проклікати вручну).

---

## Підготовка (виконує користувач)
- [ ] Створено Firebase-проєкт у Firebase Console
- [ ] Активовано Authentication (Email/Password + Google)
- [ ] Активовано Firestore Database
- [ ] Активовано Storage
- [ ] Скопійовано `firebaseConfig`
- [ ] Створено Brevo акаунт, отримано API ключ
- [ ] Створено Brevo confirmation і welcome templates (double opt-in)
- [ ] Створено 5 transactional templates для статусів замовлення (new → confirmed → in_transit → delivered, cancelled)
- [ ] Створено Brevo список підписників
- [ ] Заповнено `.env.local` усіма ключами

---

## Module 00 — Ініціалізація

### Реалізація
- [x] React + Vite ініціалізовано, Tailwind підключено
- [x] Структура папок `features/` + `shared/` створена
- [x] `shared/lib/firebase.js` працює (auth, db, storage експортуються)
- [x] `shared/lib/brevo.js` має функції `subscribe`, `confirmSubscription`, `sendTransactional`, `sendCampaign`
- [x] `AuthContext` слухає `onAuthStateChanged`, підвантажує роль
- [x] `CartContext` персистить у `localStorage`
- [x] `ToastContext` + `useToast` працюють
- [x] `ProtectedRoute` редіректить за роллю
- [x] `AppRouter` містить всі маршрути з main.md як placeholder-сторінки
- [x] Security Rules з main.md задеплоєні у Firestore і Storage
- [x] Створено першого адміна вручну (Firestore Console: users/{uid}.role = 'admin')

### Перевірки
- [x] Перехід по всіх публічних маршрутах з main.md рендерить placeholder
- [x] Без логіну `/account` редіректить на `/login`
- [x] Без `role: 'admin'` `/admin` редіректить на `/`
- [x] Перший адмін успішно потрапляє на `/admin`
- [x] Тост працює: викликати `showToast` з будь-якого місця → тост з'явився і зник через 3 сек
- [x] Кошик з тестовим item зберігається у `localStorage` після перезавантаження

---

## Module 01 — Landing + Newsletter

### Реалізація
- [x] Hero підтягує `/settings/landing.hero` з Firestore
- [x] Секція "Про нас" підтягує `/settings/landing.about`
- [x] "Наші продукти" — 4 картки `featured: true` з `/products`
- [x] "Рецепти" — 3 картки `featured: true` з `/articles`
- [x] Newsletter форма зберігає підписника зі статусом `pending`
- [x] Brevo надсилає лист підтвердження
- [x] `/subscription-confirmed` оновлює статус на `confirmed`
- [x] Footer з навігацією

### Перевірки
- [x] Створити документ `/settings/landing` з тестовими даними → Hero/Про нас рендеряться правильно
- [x] Помітити продукт `featured: true` у Firestore → з'явився на лендінгу
- [x] Newsletter end-to-end: ввести email → лист прийшов → клік по лінку → `/subscription-confirmed` показав success → статус у Firestore = `confirmed` → welcome лист прийшов
- [x] Спробувати підписатися повторно тим самим email → toast "Ви вже підписані"
- [x] Спробувати підписатися email який має статус `pending` → toast "Лист підтвердження вже надіслано"

---

## Module 02 — Auth

### Реалізація
- [x] Login: Email/Password + Google
- [x] Register: створює `/users/{uid}` з `role: 'user'`
- [x] Редірект після логіну на `state.from` або `/`
- [x] Помилки відображаються під формою (не alert)

### Перевірки
- [x] Зареєструватися новим email → `/users/{uid}` створений з `role: 'user'`
- [x] Вийти → увійти знову → працює
- [x] Увійти через Google вперше → документ у `/users` створено
- [x] Невірний пароль → текст помилки під формою (не alert)
- [x] Реєстрація з невалідними даними → submit disabled, помилки під полями
- [x] Зайти на `/account` без логіну → редірект на `/login` зі `state.from` = `/account`
- [x] Залогінитися → редірект назад на `/account`

---

## Module 03 — Магазин

### Реалізація
- [x] `/shop` — фільтри по категоріях і тегах, сортування
- [x] Skeleton loader, empty state
- [x] Картка продукту з мікровзаємодією "Додано"
- [x] `/shop/:id` — галерея, прив'язані статті
- [x] `/cart` — зміна кількості, видалення, persist
- [x] `/checkout` — Доставка/Самовивіз, валідація
- [x] Замовлення зберігається у Firestore зі статусом `new`
- [x] Brevo надсилає підтвердження клієнту
- [x] `/order/:id` — підтвердження
- [x] Кошик очищається після замовлення

### Перевірки
- [x] Додати товар у кошик → лічильник у Navbar +1, тост з'явився
- [x] Перезавантажити сторінку → кошик зберігся
- [x] Фільтр по категорії і тегу — стан в URL, працює back-navigation
- [x] Несумісний фільтр (порожній результат) → показано empty state
- [x] Продукт з `inStock: false` → кнопка "Додати" disabled
- [x] Сторінка продукту — галерея перемикається кліком на мініатюру
- [x] Checkout без логіну → редірект на `/login`
- [x] Checkout з порожнім кошиком → редірект на `/shop`
- [x] Submit checkout з порожніми обов'язковими полями → червоні рамки і повідомлення
- [x] Оформити замовлення → `/orders/{id}` створено зі статусом `new`, email прийшов клієнту, кошик очистився, редірект на `/order/:id`
- [x] Чужий `/order/:id` (через підстановку id) → доступ заборонено

---

## Module 04 — Блог

### Реалізація
- [x] `/blog` — фільтрація по тегах, картки, skeleton
- [x] `/blog/:id` — рендер rich text
- [x] Вбудовані картки продуктів у тілі статті працюють
- [x] Блок "Інші рецепти" — 3 схожі за тегами

### Перевірки
> Повноцінно тестується після Module 07. На етапі цього модуля — створити документ у `/articles` вручну через Firestore Console з body виду:
> ```html
> <h2>Тест</h2><p>Текст рецепту.</p><product-mention data-product-id="ID_ІСНУЮЧОГО_ПРОДУКТУ"></product-mention><p>Кінець.</p>
> ```
> і перевірити перші три пункти нижче. Решту відмітити після Module 07.

- [x] `/blog` — список рендериться, фільтр по тегу працює
- [x] `/blog/:id` — стаття рендериться, `<product-mention>` замінено на `InlineProductCard`
- [x] Чернетка (`published: false`) не показується у списку і не доступна за прямим URL
- [x] (після Module 07) Кнопка "Додати до кошика" з InlineProductCard у статті — працює
- [x] (після Module 07) "Інші рецепти" показує статті за перетином тегів

---

## Module 05 — Особистий кабінет

### Реалізація
- [x] `/account` — три вкладки
- [x] Замовлення: список, деталі, причина скасування
- [x] Профіль: зміна імені, телефону, пароля
- [x] Адреси: CRUD з підтвердженням видалення

### Перевірки
- [x] Зробити кілька замовлень → з'являються у вкладці "Мої замовлення"
- [x] Деталі замовлення — всі дані видно правильно
- [x] Спроба відкрити чуже замовлення `/account/orders/<чужий_id>` → редірект на `/account` (translated as: Спроба відкрити чуже замовлення `/account/orders/<чужий_id>` → редірект на `/account`)
- [x] Змінити ім'я і телефон → зберегти → перезавантажити сторінку → зберігається
- [x] Зміна пароля для email-користувача — працює (новий пароль логінить, старий — ні)
- [x] Зміна пароля для Google-користувача — показано пояснення з посиланням на Google
- [x] Додати адресу → з'являється у списку → у checkout dropdown показує її
- [x] Видалити адресу → модалка підтвердження → зникає
- [x] У checkout вибрати збережену адресу з dropdown → поля автозаповнились

---

## Module 06 — Admin Core

### Реалізація
- [x] Admin layout з бічною навігацією
- [x] Dashboard: всі картки статистики, швидкі дії
- [x] `/admin/orders` — таблиця, фільтр статусів
- [x] `/admin/orders/:id` — зміна статусу, причина скасування
- [x] При зміні статусу Brevo надсилає клієнту email

### Перевірки
- [x] `/admin` без ролі admin → редірект на `/`
- [x] З роллю admin — Dashboard показує реальну статистику з Firestore
- [x] Клік по картці "Нові замовлення" → `/admin/orders?status=new`
- [x] Швидкі дії ведуть на правильні маршрути
- [x] Створити замовлення з клієнтського акаунта → з'являється в `/admin/orders` з фільтром "Нові"
- [x] Змінити статус на `confirmed` → email прийшов клієнту з confirmation template
- [x] Спробувати зберегти статус `cancelled` без причини → submit disabled
- [x] Скасувати з причиною → клієнт бачить причину в `/account/orders/:id`
- [x] Змінити статус по черзі: new → confirmed → in_transit → delivered → отримано 4 листи

---

## Module 07 — Admin CMS
 
 ### Реалізація
- [x] `/admin/products` — CRUD, галерея зі Storage, drag-and-drop
- [x] Прив'язка статей до продукту
- [x] `/admin/blog` — CRUD, TipTap editor, нода продукту
- [x] Прив'язка продуктів до статті
 
 ### Перевірки
 
 **Товари:**
- [x] Створити продукт з 3 фото → переставити порядок drag-and-drop → зберегти → `images` у Firestore у новому порядку
- [x] Помітити `featured: true` → з'явився в "Наші продукти" на лендінгу
- [x] Inline-перемикач `inStock` у таблиці працює без переходу на форму
- [x] Прив'язати статтю до продукту → відкрити форму статті → у `linkedProductIds` з'явився цей продукт (двостороння прив'язка)
- [x] Видалити продукт → запис з Firestore і файли зі Storage прибрано
 
 **Блог:**
- [x] Створити статтю з заголовком, фото, тілом, опублікувати
- [x] У редакторі вставити `<product-mention>` через ProductPicker → у прев'ю TipTap картка рендериться
- [x] Зберегти і відкрити `/blog/:id` → InlineProductCard рендериться у тілі статті
- [x] Натиснути "Додати до кошика" з InlineProductCard у статті → товар додано, тост з'явився
- [x] Видалити продукт на який є `<product-mention>` → відкрити статтю → InlineProductCard просто не рендериться (без помилки)
- [x] Чернетка статті не з'являється на `/blog`

---

## Module 08 — Newsletter, Підписники, Налаштування, Деплой

### Реалізація
- [x] `/admin/newsletter` — compose, preview, send
- [x] Запис у `/newsletterHistory` після відправки
- [x] Історія розсилок з прев'ю
- [x] `/admin/subscribers` — таблиця, видалення
- [x] `/admin/settings` — лендінг, про нас, точки самовивозу
- [ ] **Хостинг обрано і налаштовано**
- [ ] **Production-білд задеплоєно**
- [x] **README.md написаний**

### Перевірки

**Newsletter:**
- [x] Compose: вибрати статтю → поля автозаповнились (заголовок, excerpt, фото, картки продуктів)
- [x] Прев'ю в iframe рендерить лист так, як його побачить клієнт
- [x] Кнопка "Надіслати" → модалка показує реальну кількість `confirmed` підписників
- [x] Підтвердити → лист прийшов на тестову пошту, запис створено в `/newsletterHistory`
- [x] Історія: клік по "Прев'ю" → відкриває модалку зі збереженим HTML

**Підписники:**
- [x] Лічильник зверху = кількості документів зі статусом `confirmed`
- [x] Видалити підписника → з підтвердженням, документ Firestore видалено, у Brevo списку також (якщо інтегровано)

**Налаштування:**
- [x] Змінити Hero-заголовок → зберегти → `/settings/landing.hero` оновлено → лендінг показує новий текст
- [x] Змінити фонове фото Hero → старий файл зі Storage видалено, новий завантажено
- [x] Додати точку самовивозу → з'являється у Checkout (Module 03)
- [x] Видалити точку → зникає у Checkout

**Production-деплой (фінал):**
- [x] Build (`npm run build`) проходить без помилок
- [ ] GitHub Actions workflow деплоїть успішно при push до `main`
- [ ] На production-домені працюють всі критичні шляхи: реєстрація, логін, замовлення, newsletter
- [ ] Email шаблони (newsletter confirmation, order confirmation, status change) приходять з production
- [ ] Storage файли віддаються по CDN, security rules не дають записувати не-адмінам
- [x] README актуальний (стек, env vars, Brevo налаштування, як додати адміна)
- [ ] Перший production-адмін створений, доступ передано клієнту
